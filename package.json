{
  "name": "allium-vscode-plugin-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "extensions/allium"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "test": "npm run --workspaces test",
    "lint": "npm run --workspaces lint"
  }
}
