# allium-cli

Standalone CLI package exposing:

- `allium-check`
- `allium-format`
- `allium-diagram`
- `allium-trace`
- `allium-drift`

Each CLI can load defaults from `allium.config.json` (`--config <file>` / `--no-config`), including portable project-level paths and tool-specific options.
