{
  "name": "allium-cli",
  "version": "0.1.0",
  "description": "Standalone allium-check and allium-format CLI tools.",
  "license": "MIT",
  "bin": {
    "allium-check": "./dist/src/check.js",
    "allium-format": "./dist/src/format.js",
    "allium-diagram": "./dist/src/diagram.js"
  },
  "files": ["dist/**", "README.md", "package.json", "LICENSE"],
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "test": "npm run build",
    "lint": "tsc -p ./tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
